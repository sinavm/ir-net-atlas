# مشارکت

هدف این ریپو داده تمیز است، نه کانفیگ.

- هدف جدید فقط اگر عمومی و پایدار باشد به `targets.json` اضافه شود.
- پروب باید بدون کلید خصوصی کار کند.
- ادعا نکنید رانر گیتهاب یعنی «از داخل ایران».
